package cn.org.autumn.auth.scope;

/**
 * userInfo 输出字段（JSON key 与枚举名一致，单词）。
 */
public enum AuthField {
    uuid,
    openId,
    unionId,
    nickname,
    icon,
    username,
    mobile,
    email,
    verified,
    status
}
